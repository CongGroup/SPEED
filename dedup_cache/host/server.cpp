#include "server.h"

#include "Enclave_t.h"

void init_server() {
    // remember to register signal handler
}

void run_server() {
    while (true) {
        // on receiving request (depend on how we handle networking)

        // parse request

        // make ECALL ...

        // prepare response

        // send out response
    }
}